<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.riac.marketapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- I DO NOT RECOMMEND TO USE  android:usesCleartextTraffic="true" BECAUSE OF SECURITY REASONS
     BUT OTHERWISE IMAGES WILL NOT BE ABLE TO BE SHOWN-->
    <application
        android:usesCleartextTraffic="true"
        android:name=".MarketApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarketApp">
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MarketApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>